<p id="notice"><%= notice %></p>

<p>
  <h2>
  <%= @user.username %></h2>

</p>
 <% if @user.ratings.empty? %>
 user has not yet been rating beers!
<% else %>
  has made
<%= pluralize(@user.ratings.count, 'rating') %>, average
<%= @user.average_rating %>
<% end %>
<br/>
<p>
<table>
<thead>
<tr>
<th>Ratings</th>
<th></th>
</tr>
</thead>
<tbody>
 <% @user.ratings.each do |rating| %>
 <tr>
   <td><%= rating %><br/></td>
<% if @user == current_user %>
   <td> <%= link_to 'Delete', rating, method: :delete, data: { confirm: 'Are you sure?' } %></td>
<% end %>
   <tr>
 <% end %>
 </tbody>
 </table>
<table>
<thead>
<tr>
<th>Olutseurat</th>
</tr>
</thead>
<tbody>
 <% @user.memberships.each do |membership| %>
 <tr>

   <td><%= membership %><br/></td>

  
   <tr>
 <% end %>
 </tbody>
 </table>
</p>
<%= link_to 'Edit', edit_user_path(@user) %> |
<% if not current_user.nil? %>
<%= link_to 'Destroy user', @user, method: :delete, data: { confirm: 'Are you sure?' } %><% end%> |
<%= link_to 'Back', users_path %>
