<!DOCTYPE html>
<html>
<head>
  <title>Ratebeer</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<% user = current_user %>
  <div class="navibar">
    <%= link_to 'breweries', breweries_path %>
    <%= link_to 'beers', beers_path %>
    <%= link_to 'ratings', ratings_path %>
    <%= link_to 'users', users_path %>
    <%= link_to 'join to a beer club', new_beer_club_path %>
|
	<% if not current_user.nil? %>
          <%= link_to current_user.username, user %>
          <%= link_to 'rate a beer', new_rating_path %>
 	   <td><%= link_to 'sign out', signout_path, method: :delete, data: { confirm: 'Are you sure?' } %></td> 
	
       <% end %>
	<% if current_user.nil? %>
    <%= link_to 'sign in', new_session_path %>
    <%= link_to 'sign up', new_user_path %>
	<% end %>
    
  </div>
<%= yield %>

<% if not current_user.nil? %>
  <p><%= current_user.username %> signed in</p>
<% end %>

</body>
</html>
